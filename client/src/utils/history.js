//From Auth0 boilerplate-we think this takes users browser history to see if they've logged in before and keeps login until manually logged out
import { createBrowserHistory } from "history";
export default createBrowserHistory();